so let suppose we are on master ans we want to merge the another temp branch into the merge one
We use git merge <branch name>
uske bad confict resolve kro

then add kro
then merge kro
commit krne se merge ho jayega

//to check merged and non merged branch we use

- git branch --merger
- git branch --no-merged
